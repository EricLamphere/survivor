% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_init.R
\name{get_season_name_tbl}
\alias{get_season_name_tbl}
\title{Get Season Names}
\usage{
get_season_name_tbl(seasons, picks = NULL)
}
\arguments{
\item{seasons}{Integer vector of season numbers to look up}

\item{picks}{Optional data frame with \code{season} and \code{season_name} columns
used as the first-pass source. If \code{NULL}, uses the global \code{season_picks}.}
}
\description{
Two-pass lookup for season subtitle(s). The first pass checks the provided
\code{picks} data frame (Google Sheet data). The second pass falls back to
\code{survivoR::season_summary} for any seasons not found in the first pass.
}
\details{
Returns a tibble with \code{season} (integer) and \code{season_name} (subtitle, with
the "Survivor: " prefix already stripped). Seasons with no known subtitle
are excluded.
}
