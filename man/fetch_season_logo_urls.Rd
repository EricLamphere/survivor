% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_init.R
\name{fetch_season_logo_urls}
\alias{fetch_season_logo_urls}
\title{Fetch Season Logo URLs from Fandom Wiki}
\usage{
fetch_season_logo_urls(seasons = unique(season_picks$season))
}
\arguments{
\item{seasons}{Integer vector of season numbers to fetch. Defaults to all
seasons in the global \code{season_picks}.}
}
\description{
Makes a batch imageinfo API call to survivor.fandom.com to get CDN URLs for
each season's logo. The standard filename \code{US_SN_logo.png} is tried first.
For named seasons where that file doesn't exist (e.g. season 50), the
subtitle from \code{\link[=get_season_name_tbl]{get_season_name_tbl()}} is used to derive the filename
(e.g. "In the Hands of the Fans" -> \code{In_the_Hands_of_the_Fans_Logo.png}).
}
\details{
Returns a named list of season number (as string) -> CDN URL, suitable for
use directly in \code{shiny::img(src = ...)}.
}
